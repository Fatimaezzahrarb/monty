#include "monty.h"
/**
 * f_stack - prints in the top.
 * @head: stack head
 * @counter: line.number
 * Return: NO return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
